# 포트폴리오 표현 노트

자소서/면접에서 이 프로젝트를 설명할 때 과장 없이 정확하게 말할 수 있도록 정리한 메모.

## 정확한 표현 (이렇게 쓸 것)

- ❌ "LLM을 직접 구현했다" / "AI 모델을 개발했다"
- ✅ "오픈소스 LLM(Qwen2.5-3B-Instruct)을 로컬 환경(CPU, 4bit 양자화)에 서빙하고, 프롬프트
  엔지니어링으로 뉴스 감정점수화 파이프라인을 구축했다"
- ✅ "규칙기반(사전+형태소분석)과 LLM 두 가지 감정분석 방식을 병렬로 운영하고 정량적으로
  비교(일치율 54.2%)해, 각 방식의 강점과 한계를 데이터로 검증했다"
- ❌ "AI가 주가를 예측한다"
- ✅ "가격 기술지표(이동평균, 변동성)와 뉴스 감정 신호를 결합해 참고용 시그널을 만들었다 —
  실제 예측 정확도는 baseline(무변동 가정)을 넘지 못했고, 이 한계를 METRICS.md에 그대로 기록했다"

## MVP에서 실제로 완료한 것

- 주가(yfinance)/뉴스(Selenium+Naver) 수집·저장 파이프라인 — 실행 검증 완료 (각 64건/24건)
- 공시(OpenDART) 수집 코드 — 완성, API 키 발급 후 최종 실행 검증 예정
- 정규식 정제 + Kiwi 형태소분석 기반 규칙기반 감정사전 구축 (직접 큐레이션한 한국어
  금융뉴스 사전, 약 70개 어휘)
- Ollama 로컬 LLM 제로샷 프롬프트 감정점수화 — 라벨링 데이터셋/파인튜닝 없이 동작
- scikit-learn 회귀모델(Ridge, RandomForest) 학습·평가, baseline과 RMSE 비교
- 규칙기반 vs LLM 비교 분석 — 단순 수치 비교를 넘어 "왜 다른가"를 설명 (채용/공채 같은
  정형화된 홍보성 기사에서 규칙기반은 사전 매칭 실패로 중립 판정, LLM은 문맥 이해로 방향성
  제시 — 대신 LLM은 일상적 홍보 기사를 과신해서 긍정으로 과대평가하는 경향도 함께 관찰)
- 가격신호+감정신호 결합 미니 프로토타입 (가중합 기반 종합 시그널)
- Streamlit 대시보드, 마크다운 리포트 자동 생성, METRICS.md 자동 갱신, 실행마다 git commit

## 확장 범위 진행 상태

- **Docker 컨테이너화**: **완료, 실제 구동 검증까지 함.** `Dockerfile` + `docker-compose.yml`로
  dashboard/scheduler/ollama 3개 서비스를 정의했고, 실제로 이미지를 빌드(2.65GB)하고
  `docker compose up`으로 띄운 뒤 브라우저로 대시보드가 컨테이너 안에서 SQLite 데이터를
  읽어 정상 렌더링되는 것까지 확인함.
- **Kubernetes**: **매니페스트 작성 + 오프라인 스키마 검증까지 완료, 실제 클러스터 배포는
  미완료.** `k8s/`에 ConfigMap/Secret/CronJob/Deployment/Service/PVC 매니페스트를 작성했고,
  `kubeconform`(오프라인 K8s 스키마 검증 도구)으로 5개 파일 9개 리소스 전부 스키마 검증을
  통과시켰다. 다만 이 환경에는 실제로 붙일 수 있는 K8s 클러스터가 없어 `kubectl apply`로
  실제 배포해 동작을 확인하지는 못했음 — 이 점을 숨기지 않고 README/METRICS에도 동일하게
  기록함. MVP 완성 상태의 가치를 이 항목의 미완료가 훼손하지 않도록 별도로 기록.
- **GitHub Actions CI**: **린트/테스트 워크플로는 완료, 로컬에서 ruff+pytest 통과 확인.**
  `pytest` 16개 테스트(규칙기반 감정점수화, 텍스트 정제, 날짜 파싱, 시그널 라벨링 — 외부
  API/브라우저 의존 없는 순수 로직만) 작성, 테스트를 작성하는 과정에서 실제 버그 2건을
  발견해 고쳤음(아래 참고). "리포트 자동 커밋" 워크플로(`report.yml`)도 작성했지만
  `workflow_dispatch`(수동 트리거) 전용으로 두었고, **GitHub 인프라에서 실제로 실행해
  검증하지는 못했다** — Ollama 설치+모델 pull 단계가 가장 실패 가능성이 높은 지점.
- **KcELECTRA 파인튜닝 비교**: 미착수 — zero-shot LLM 방식만 검증
- **다른 종목으로 재사용성 검증**: 미착수. `.env`의 TICKER만 바꾸면 동작하도록 설계했으나,
  실제로 다른 종목을 넣어 끝까지 돌려본 적은 없음 (설계 의도이지 검증된 사실 아님)

## 테스트 작성 중 발견해서 고친 실제 버그 (정직하게 기록 — 과장 없이 이 자체가 좋은 소재)

1. **부정어 탐지 방향 오류**: "실적이 개선되지 않았다" 같은 `-되지 않다` 부정 표현에서
   부정어("않")가 감정 단어보다 *뒤*에 오는데, 기존 코드는 *앞* 방향만 검사해서 부정문을
   긍정으로 잘못 판단하고 있었음. 테스트를 먼저 짜다가 발견해서 양방향 검사로 수정.
2. **기자 바이라인 제거 정규식이 실제로 한 번도 안 맞고 있었음**: `(...기자)`처럼 괄호
   안에서 끝나는 패턴을 가정했는데, 실제 연합뉴스 등의 바이라인은 `(서울=연합뉴스) 홍길동
   기자 = 본문...`처럼 기자 이름이 괄호 *밖*에 있음. 정규식을 실제 관측 패턴에 맞게 재작성.

## 타겟 직무별 각도 메모

### (a) 핀테크 / 증권사 / 자산운용 / AI-ML 엔지니어 지원 시
예측모델 + 로컬 LLM + MLOps 스토리를 정면으로 강조:
- "제로 예산·CPU 전용 환경에서 로컬 LLM 서빙과 전통 ML 파이프라인을 통합 설계·구현"
- "규칙기반 대비 LLM의 정량적 성능 차이를 스스로 실험 설계해 검증"
- "Docker로 컨테이너화하고 실제 구동까지 확인, K8s 매니페스트를 작성해 스키마 검증까지
  마쳤다" — 단, K8s는 실제 클러스터 배포까지는 못 했다는 사실을 흐리지 말고 정확히 구분해서
  말할 것 (설계+검증 완료 vs. 실배포 완료는 다른 주장)
- "테스트를 먼저 작성하면서 기존 감정분석 로직의 버그 2건(부정어 처리 방향, 정규식 패턴
  오류)을 실제로 발견해 수정했다" — 테스트가 형식적 커버리지가 아니라 실제 버그를 잡는
  도구로 쓰였다는 근거로 제시 가능

### (b) 정책금융기관 (여신심사 등) 지원 시
"가격예측" 자체보다 데이터 기반 의사결정 역량과 학습 능력 쪽으로 각도를 틀 것:
- "여러 이질적 데이터 소스(정형 주가, 반정형 공시, 비정형 뉴스)를 하나의 의사결정 파이프라인으로
  통합한 경험"
- "예산 제약 하에서 신기술(로컬 LLM)을 스스로 학습하고 실전 문제에 적용해본 경험 — 새로운
  방법론 도입 시 기존 방식(규칙기반)과 반드시 정량 비교해 검증하는 습관"
- 예측모델이 baseline을 넘지 못했다는 결과를 숨기지 말고, "모델 결과를 있는 그대로 보고하고
  한계를 인지하는 태도"로 재구성해서 말할 것 — 여신심사 직무에서는 이 쪽이 오히려 강점
