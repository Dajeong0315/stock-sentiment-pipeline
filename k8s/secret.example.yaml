# Template only — copy to secret.yaml, fill in the real key, and DO NOT commit secret.yaml.
# Applied separately from configmap.yaml because this one holds a credential.
apiVersion: v1
kind: Secret
metadata:
  name: stock-sentiment-secret
type: Opaque
stringData:
  OPENDART_API_KEY: "REPLACE_ME"
